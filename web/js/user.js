var sw = sw || {};
sw.user = sw.user || {};

(function(sw, $) {
  
  //TODO add a minimum level property and the option to specify an array of levels in case some levels should be skipped
  sw.user.name = 'maddy';
  sw.user.level = 7;
  
})(sw, jQuery);